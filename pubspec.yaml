name: helper
description: Common helper that every project needs. CurrencyHelper, DateTimeHelper, FileHelper, DialogHelper
version: 0.0.12
author: HaniHashemi <jhanihashemi@gmail.com>
homepage: https://github.com/hanihashemi/Helper

environment:
  sdk: ">=2.0.0-dev.68.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.15.7
  cached_network_image: ^0.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter: